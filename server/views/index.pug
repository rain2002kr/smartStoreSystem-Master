extends layout

block content
  h1= title
  
  p Welcome to #{title}
  //- var target = 'users'

  a(href = PORT + 'users/orderList' class="link") 조리 현황
  br
  a(href = PORT + 'example' class="link") PUG 테스트 
  br
  a(href = PORT + 'users' class="link") 회원 관리
  br
  a(href = PORT + 'users' class="link") 메뉴 관리    
  br
  a(href = PORT + 'hoonsDB' class="link") 몽구스 관리자    
  br
  

  p 음식 메뉴 리스트 from MongoDB 
  each elem,i in json
    p !{elem.__v} 
    p !{elem.email}-!{elem.name}-!{elem.age}-!{elem.createdAt}-!{elem.updatedAt}
  br

  //a(href='google.com') Google
  //a(class='button', href='google.com') Google
  //div(escaped="<code>")
  //div(unescaped!="<code>")
  //!{elem.id}-
  